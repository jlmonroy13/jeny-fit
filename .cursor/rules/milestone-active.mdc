---
description: Active milestone for Jeny Fit — current issues and next actions.
alwaysApply: true
---

# Active milestone (Jeny Fit)

**Current:** **M1 · Foundation** ✅ cerrada (M1-01…M1-06 + M1.10). **Siguiente:** detallar **M2 · Auth & onboarding** con skill `roadmap-detail`, luego `roadmap-sync` → Ready → code. Do not implement M2 until issues exist (**RM-003**).

## Binding now

| Issue | Status | Next action |
|-------|--------|-------------|
| **M1** | ✅ Closed | Retrospectiva M1.10 en doc 12 |
| **M2** | Esqueleto | `/roadmap-detail` M2 → sync → Ready |
| **M3+** | Esqueleto | Solo tras M2.10 + `roadmap-detail` |

## Rules

1. **One issue = one PR** (**RM-001**); branch `M2-NN-slug` (**RM-010**) when M2 is detailed.
2. Prefer `roadmap-sync` → Ready → code when using GitHub Project (**RM-006**).
3. **M3+** remains skeleton — no build ahead (**RM-003**).
4. Respect **TS-026** (free tier Vercel/Neon).
5. Before coding: read **Acceptance criteria** and **Out of scope** in the detailed issue.
6. Honor **M1.10 lecciones** in doc 12 (seed coach temprano, no CI placeholders, AC = título).

When M2 issues are synced and work starts, update this rule’s “Current” line to **M2**.
