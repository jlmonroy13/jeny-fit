---
description: Active milestone for Jeny Fit — current issues and next actions.
alwaysApply: true
---

# Active milestone (Jeny Fit)

**Current:** **M2 · Auth & onboarding** — detalle en [`docs/product/12-roadmap-milestones.md`](../../docs/product/12-roadmap-milestones.md) §6. **M1** cerrada. Do not build M3+ (**RM-003**).

## Binding now

| Issue | Status | Next action |
|-------|--------|-------------|
| **M1** | ✅ Closed | — |
| **M2-01…M2-17** | Detalle §6 | `roadmap-sync` → Ready → implement |
| First to code | **M2-01** (Drizzle/Neon) | `Depends on: none` (paralelo: **M2-08** primitives) |
| **M3+** | Esqueleto | Solo tras M2.10 + `roadmap-detail` |

## Rules

1. **One issue = one PR** (**RM-001**); branch `M2-NN-slug` (**RM-010**).
2. Prefer `roadmap-sync` → Ready → code when using GitHub Project (**RM-006**).
3. **M3+** remains skeleton — no build ahead (**RM-003**).
4. Respect **TS-026** (free tier Vercel/Neon); Postgres service for integration (`TEST-016`).
5. Before coding: read **Acceptance criteria** and **Out of scope** in the detailed issue.
6. Honor **M1.10 lecciones** (seed coach temprano, no CI placeholders, AC = título).

When M2 closes (after M2.10), update this rule’s “Current” line to M3.
